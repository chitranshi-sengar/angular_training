import { RouterConfig } from '../interfaces/router-config.interface';

export const routerConfig: RouterConfig = {
  products: {
    base: 'products'
  },
  timer: {
    base: 'timer'
  },
  timer_subject: {
    base: 'timer-subject'
  },
  students: {
    base: 'students'
  },
  dynamic_div: {
    base: 'dynamic-div'
  },
};
